import { TextDocument } from 'vscode';
export interface ContentItem {
    id: string;
    text: string;
}
export declare class Util {
    static getContentItemsFromDocument(document: TextDocument): Promise<ContentItem[]>;
    static getContentItems(manifest: any, srcDirectoryPath: string): Promise<ContentItem[]>;
}
