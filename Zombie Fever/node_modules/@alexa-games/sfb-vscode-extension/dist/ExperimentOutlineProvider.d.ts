import * as vscode from 'vscode';
export declare class ExperimentOutlineProvider implements vscode.TreeDataProvider<any> {
    private context;
    private _onDidChangeTreeData;
    readonly onDidChangeTreeData: vscode.Event<any | null>;
    tree: any;
    private text;
    private editor;
    constructor(context: vscode.ExtensionContext);
    refresh(offset?: any): Promise<void>;
    private onActiveEditorChanged;
    private onDocumentChanged;
    private parseTree;
    getChildren(scene?: any): Thenable<any[]>;
    getTreeItem(scene: any): vscode.TreeItem;
    select(range: vscode.Range): void;
}
