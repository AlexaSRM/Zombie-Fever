/**
 * Leaving the original function here for the time being.  Unit tests are calling it to demonstrate
 * compatibility.
 */
export declare function substituteSlotValues(line: string, eventParams: any, isCondition?: boolean): any;
