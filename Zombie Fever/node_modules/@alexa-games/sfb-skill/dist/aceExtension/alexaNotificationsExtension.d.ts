import { DriverExtension, InstructionExtension, DriverExtensionParameter } from '@alexa-games/sfb-f';
import { ConfigAccessor } from './../configAccessor';
export declare class AlexaNotificationsExtension implements DriverExtension, InstructionExtension {
    constructor(locale: string, configAccessor: ConfigAccessor);
    post(param: DriverExtensionParameter): Promise<void>;
    pre(param: DriverExtensionParameter): Promise<void>;
    showNotificationsPermissionsCard(param: DriverExtensionParameter): Promise<void>;
}
