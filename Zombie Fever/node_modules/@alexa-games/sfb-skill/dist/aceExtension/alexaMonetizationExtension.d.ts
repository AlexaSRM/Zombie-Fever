import { DriverExtension, InstructionExtension, DriverExtensionParameter, InstructionExtensionParameter } from '@alexa-games/sfb-f';
import { ConfigAccessor } from './../configAccessor';
export interface ISPItem {
    ISPID: string;
    productName: string;
}
export declare class AlexaMonetizationExtension implements DriverExtension, InstructionExtension {
    private enterMonetization;
    private workflowType;
    private activeProductISP;
    private productToISP;
    constructor(locale: string, configAccessor: ConfigAccessor);
    post(param: DriverExtensionParameter): Promise<void>;
    pre(param: DriverExtensionParameter): Promise<void>;
    buy(param: InstructionExtensionParameter): Promise<void>;
    refund(param: InstructionExtensionParameter): Promise<void>;
    private registerMonetizationChoices;
    private parseAlexaMonetizationResponse;
    private generateBuyDirective;
    private generateCancelDirective;
}
