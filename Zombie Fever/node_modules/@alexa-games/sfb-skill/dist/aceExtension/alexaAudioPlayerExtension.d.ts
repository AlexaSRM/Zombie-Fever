import { DriverExtension, InstructionExtension, DriverExtensionParameter, InstructionExtensionParameter } from '@alexa-games/sfb-f';
import { ConfigAccessor } from './../configAccessor';
export interface AudioPlayerItem {
    url: string;
    playBehavior?: string;
    offsetInMilliseconds?: number;
    metadata?: {
        title?: string;
        subtitle?: string;
        art?: string;
        backgroundImage?: string;
    };
}
export declare enum AudioPlayerDirective {
    Play = "AudioPlayer.Play",
    Stop = "AudioPlayer.Stop",
    ClearQueue = "AudioPlayer.ClearQueue",
    None = "None"
}
export declare class AlexaAudioPlayerExtension implements DriverExtension, InstructionExtension {
    private directivesToPost;
    constructor(locale: string, configAccessor: ConfigAccessor);
    post(param: DriverExtensionParameter): Promise<void>;
    generateAudioPlayerDirective(directive: AudioPlayerDirective, audioPlayerItem?: AudioPlayerItem, storyState?: {
        [key: string]: any;
    }): any;
    pre(param: DriverExtensionParameter): Promise<void>;
    audio_player_play(param: InstructionExtensionParameter): Promise<void>;
    audio_player_stop(param: InstructionExtensionParameter): Promise<void>;
    audio_player_clear_queue(param: InstructionExtensionParameter): Promise<void>;
}
