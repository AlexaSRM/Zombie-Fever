import { Logger } from './logger';
import { StdOutput } from './stdOutput';
import { Command } from './command';
export declare class ImportCommand implements Command {
    private readonly storyPath;
    private readonly enforceLatest;
    private readonly logger;
    private readonly stdOutput;
    constructor(storyPath: string, enforceLatest: boolean, logger: Logger, stdOutput: StdOutput);
    run(): Promise<void>;
    private versionChecks;
    private checkOnlineForNewerTooling;
    private compileCode;
    private copyAbcConfig;
    private reportErrors;
    private static isImportError;
    private static isImportErrorObject;
    private copyGeneratedResources;
    private importStory;
    private getCombinedStoryContentFromManifest;
    private importAdditionalDirectories;
    private importResourceFiles;
}
