import { Command } from './command';
import { Logger } from './logger';
export declare class BakeCommand implements Command {
    private readonly storyPath;
    private readonly logger;
    constructor(storyPath: string, logger: Logger);
    run(): Promise<void>;
    private copyContent;
    bakeAPLFiles(aplTemplatesFilePath: string, outputAPLFilePath: string): Promise<void>;
}
