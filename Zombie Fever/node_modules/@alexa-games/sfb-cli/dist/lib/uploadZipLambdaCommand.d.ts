import { Logger } from './logger';
import { StdOutput } from './stdOutput';
import { Command } from './command';
export declare class UploadZipLambdaCommand implements Command {
    private readonly storyPath;
    private readonly logger;
    private readonly locale;
    private readonly stdOutput;
    constructor(storyPath: string, logger: Logger, locale: string, stdOutput: StdOutput);
    run(): Promise<void>;
}
