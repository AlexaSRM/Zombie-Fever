import { AudioFileAccessor } from './audioFileAccessor';
import { S3 } from 'aws-sdk';
export declare enum S3FileStatus {
    Unknown = 0,
    Exists = 1,
    NotExists = 2
}
export declare const s3FileStatusStaticCache: {
    [key: string]: S3FileStatus;
};
/**
 * Implentation of AudioFileAccessor to use S3 as cache storage.
 */
export declare class S3AudioAccessor implements AudioFileAccessor {
    private setting;
    static readonly CACHE_DIR_NAME = "pollyCache";
    private static s3Client;
    constructor(setting: {
        bucketName: string;
        s3DomainName: string;
        audioWorkingDir: string;
        s3Client?: S3;
    });
    exists(audioName: string): Promise<boolean>;
    getAudioURL(audioName: string): Promise<string>;
    uploadAudio(audioName: string, workingDirectory: string): Promise<string>;
    downloadAudio(audioUrl: string, workingDirectory: string): Promise<void>;
}
