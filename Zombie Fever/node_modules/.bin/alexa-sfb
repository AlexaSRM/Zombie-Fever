#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@alexa-games/sfb-cli/dist/bin/alexa-sfb" "$@"
  ret=$?
else 
  node  "$basedir/../@alexa-games/sfb-cli/dist/bin/alexa-sfb" "$@"
  ret=$?
fi
exit $ret
