import { AudioFileAccessor } from './audioAccessor/audioFileAccessor';
import { Polly } from 'aws-sdk';
export interface PollyRequestItem {
    name: string;
    text: string;
    delayMs: number;
    engine: string;
    volume: string;
    rate: string;
    pitch: string;
    speechMarkTypes?: [string];
}
export declare enum PollyOutFormat {
    MP3 = "mp3",
    JSON = "json"
}
export interface PollyConfig {
    dontUseCache?: boolean;
    dontCombineIfAudioTagSeperatedBySpace?: boolean;
    combineAudioTags?: boolean;
    dontUploadToS3?: boolean;
    awsProfileName?: string;
    awsRegion?: string;
    cognito?: {
        identityPoolId: string;
        loginWithAmazonAccessToken?: string;
    };
    [key: string]: any;
}
export declare class PollyUtil {
    private audioAccessor;
    private static pollyClient;
    static pollyConfig: PollyConfig;
    constructor(audioAccessor: AudioFileAccessor, pollyClient?: Polly, awsRegion?: string);
    static configurePolly(config: PollyConfig): void;
    synthesize(request: PollyRequestItem, workingDir: string, filename: string, outputFormat?: PollyOutFormat): Promise<any>;
    estimateSSMLDuration(ssml: string, workingDir: string): Promise<number>;
    calculateMP3Duration(filePath: string): Promise<number>;
    private estimateNonAudioTagDuration;
}
