import { DriverExtension, DriverExtensionParameter, InstructionExtension, InstructionExtensionParameter } from '@alexa-games/sfb-f';
import { HandlerInput } from 'ask-sdk-core';
interface MetricsEvent {
    skillId: string;
    timestamp: string;
    customerIdentifier: string;
    eventType: string;
    requestId: string;
    eventId: string;
    sessionId: string;
    stage: string;
    locale: string;
    metadata: {
        schemaVersion: string;
    };
    details: {
        [key: string]: any;
    };
}
export declare class MetricsExtension implements DriverExtension, InstructionExtension {
    private metricEvent;
    private reservedKeys;
    constructor(sendMetricsFunction?: (metricEvent: MetricsEvent) => Promise<void>);
    post(param: DriverExtensionParameter): Promise<void>;
    addRequestParameters(request: HandlerInput | undefined): void;
    pre(param: DriverExtensionParameter): Promise<void>;
    trackMetric(param: InstructionExtensionParameter): Promise<void>;
    private checkForDetailOverwrite;
    private canWriteKey;
    private sendMetricsEvent;
    private getLast;
    private logPreviousScenes;
    private getDefaultMetricsEvent;
}
export {};
